<block wx:if="{{$taroCompReady}}">
    <view class="wrap">
        <view class="from">
            <view class="fromItem">
                <input value="{{name}}" type="text" placeholder="请输入姓名" bindinput="anonymousFunc0" />
            </view>
            <view class="fromItem">
                <input value="{{age}}" type="text" placeholder="请输入年龄" bindinput="anonymousFunc1" />
            </view>
        </view>
        <view class="btn bg_fa d_flex fd_r ai_ct jc_ct mt_20" bindtap="_add"><text class="white_label default_label">增加</text>
        </view>
    </view>
</block>